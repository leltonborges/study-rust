mod week_days;
mod matrix;
mod colors;
mod option;
mod vertors;

use colors::{color, color::Color};
use matrix::matriz;
use week_days::week_and_days;
use option::conteudo;
use vertors::vect;

fn main() {
    // arrays();
    // matrix();
    // week_and_days();
    // conteudo();
    vect();
}